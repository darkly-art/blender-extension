schema_version = "1.0.0"

id = "darkly_stream"
version = "0.3.0"
name = "Darkly Stream"
tagline = "Stream the viewport or a camera view into a Darkly layer"
maintainer = "TheTechromancer <thetechromancer@protonmail.com>"
type = "add-on"
website = "https://darkly.art"

tags = ["Import-Export"]

blender_version_min = "5.1.0"

# Blender's extension platform only accepts GPL-family licenses (add-ons link
# Blender's GPL Python API); AGPL is rejected by the manifest validator.
license = ["SPDX:GPL-3.0-or-later"]

# Keep dev/build artifacts and tests out of the shipped zip.
[build]
paths_exclude_pattern = [
  "__pycache__/",
  "*.pyc",
  "/tests/",
]
